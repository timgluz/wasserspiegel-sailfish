# Copy to .envrc and fill in the values, then `source .envrc`
# (or use `direnv allow .`).

# wasserspiegel API (Fermyon Spin service)
export WASSERSPIEGEL_API="https://wasserspiegel-hnhptksa.fermyon.app"
export WASSERSPIEGEL_TOKEN=""

# Phone developer password (Settings -> Developer tools -> Remote connection)
export JOLLA_SSH_PASSWORD=""
